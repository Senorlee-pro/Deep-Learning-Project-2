# Deep-Learning-Project-2

## Environment Setup
For environment setup, you can create a `conda` environment using `requirements.txt`.

## Data
Put the **CIFAR-10** data folder at:
```
PJ2/
├── data/                              # CIFAR-10
│   └── cifar-10-batches-py/
├── task1/
├── task2/
│   └── VGG_BatchNorm/
└── requirements.txt
```
## Get Started
For task 1, you can run `main.py` under `task1`.

For task 2, you can run `main.py` under `task2/VGG_BatchNorm`.
